import os, sys
def checkcoala():
	""" It is a dummy method. """
	a = 'valid string'
	b = "again valid string"
	print(a+b+"another addition to the set of valid strings, wow")
	c = True
	if c:
		if 'valid' in a or 'valid' in b or len(a) + len(b) > 9 or 'again' in valid:
			     print('Its valid')